@model LivrosViewModel

@{
    ViewData["Title"] = "Index";
}

<br />
<div>
    <h2>@Model.CategoriaAtual</h2>
</div>


<div class="container">
    <div class="row">

        @foreach (var item in Model.Livros)
        {
            <partial name="_LivrosResumo" for="@item" />

            
        }

    </div>

</div>